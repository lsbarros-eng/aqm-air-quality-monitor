# AQM — example local credentials
# Copy this file as secrets.yaml and replace the placeholders.
# Never commit secrets.yaml to a public repository.

wifi_ssid: "YOUR_WIFI_NAME"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_password: "CHANGE_THIS_PASSWORD"
